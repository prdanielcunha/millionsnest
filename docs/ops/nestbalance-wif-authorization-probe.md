# NestBalance WIF authorization probe

Temporary no-op marker used only to trigger the tightly scoped `pull_request_target` infrastructure workflow that authorizes NestBalance homologation in the existing MillionsNest Workload Identity provider. This change is not intended to merge into `production`.
