{
  "entities": {
    "User": {
      "title": "User Profile",
      "description": "Centralized user profile for MillionsNest ecosystem.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "name": { "type": "string" },
        "displayName": { "type": "string" },
        "photoURL": { "type": "string" },
        "products": { "type": "array", "description": "Managed by backend. e.g. ['musicscale']" },
        "organizationId": { "type": "string", "description": "Currently active organization workspace ID." },
        "lastLoginAt": { "type": "string", "format": "date-time" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "email"]
    },
    "Organization": {
      "title": "Organization",
      "description": "Multi-tenant entity representing a church or band.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "ownerUid": { "type": "string" },
        "plan": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },
    "Subscription": {
      "title": "Subscription",
      "description": "Stripe managed subscription status linked to a user.",
      "type": "object",
      "properties": {
        "product": { "type": "string" },
        "status": { "type": "string" },
        "stripeCustomerId": { "type": "string" },
        "stripeSubscriptionId": { "type": "string" },
        "plan": { "type": "string" },
        "currentPeriodEnd": { "type": "string", "format": "date-time" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["status", "stripeSubscriptionId"]
    },
    "organization_members": {
      "title": "Organization Membership",
      "description": "Global link between users and organizations with roles.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "organizationId": { "type": "string" },
        "role": { "type": "string", "enum": ["owner", "admin", "member"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "organizationId", "role"]
    }
  },
  "firestore": {
    "users/{userId}": {
      "schema": "User",
      "description": "Primary user documents."
    },
    "organizations/{orgId}": {
      "schema": "Organization",
      "description": "Organization details."
    },
    "organization_members/{memberId}": {
      "schema": "organization_members",
      "description": "Global membership records."
    },
    "subscriptions/{uid}": {
      "schema": "Subscription",
      "description": "Global subscriptions map linked to users."
    }
  }
}
